apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: wordpress
spec:
  resources:
    requests:
      storage: {{ .Values.storage }}
  accessModes:
    - ReadWriteOnce
